# LLM_stock_pricing

Это репозиторий проекта по портфельному менеджементу с помощью ML моделей.

Метрики приведены в ноутбуках src/baseline/baseline.ipynb и src/llm/metric_eval.ipynb

Запуск обучения pip install requirements.txt && cd src/llm && python train.py
